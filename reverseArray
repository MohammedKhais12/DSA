class Solution {
public:
    void reverseString(vector<char>& s) {
        char temp;
        for(int i=1; i<=s.size()/2; i++){
            temp = s[i-1];
            s[i-1] = s[s.size()-i];
            s[s.size()-i] = temp;
        }
    }
};
