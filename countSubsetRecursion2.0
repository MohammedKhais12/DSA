// class Solution {
// public:

//     void summ(int i, int n, int sum,
//               vector<int>& arr, int& count, int k) {

//         // Reached the end
//         if (i == n) {
//             if (sum == k)
//                 count++;

//             return;
//         }

//         // TAKE arr[i]
//         summ(i + 1, n, sum + arr[i], arr, count, k);

//         // DON'T TAKE arr[i]
//         summ(i + 1, n, sum, arr, count, k);
//     }

//     int countSubset(vector<int>& arr, int k) {

//         int count = 0;

//         summ(0, arr.size(), 0, arr, count, k);

//         return count;
//     }
// };
