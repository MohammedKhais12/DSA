
class Solution {
  public:
    vector<int> printList(Node *head) {
        // code here
        vector <int> vec;
        Node* temp = head;
        while(temp->next!=NULL){
            
            vec.push_back(temp->data);
            temp = temp->next;
            
        }
        vec.push_back(temp->data);
        return vec;
    }
};
