class Solution {
public:
    int strStr(string haystack, string needle) {
        int ind = haystack.find(needle);
        if(ind<=haystack.size()){
            return ind;
        }
        return ind;
    }
};
