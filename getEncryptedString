class Solution {
public:
    string getEncryptedString(string s, int k) {
        string str = "";
        int n = s.size();
        for (int i = 0; i < n; i++) {
            int j = (i + k) % n;   // wrap around using modulo
            str += s[j];
        }
        return str;
    }
};
